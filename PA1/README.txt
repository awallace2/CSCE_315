For this assignment we were given a list of servers and asked to query them and record their response time. After finding the time, we record the discrepancy of all the servers and return the server with the greatest discrepancy, ignoring servers that took longer than 5 seconds to respond.

This program executes as intended simply by running the file using IDLE. No external dependencies were used or required to run.
